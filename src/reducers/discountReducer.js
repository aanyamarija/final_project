
const initialState = {
    discounts: [],
}

const discountReducer = (state = initialState, action) =>{
    switch (action.type){
        default:
            return state;
    }
}

export default discountReducer;